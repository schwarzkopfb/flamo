{
  "name": "etelbar",
  "version": "0.0.1",
  "scripts": {
    "start": "./node_modules/.bin/http-server ./public -s -d false -p ${PORT:-8080}"
  },
  "dependencies": {
    "http-server": "^0.10.0"
  }
}
